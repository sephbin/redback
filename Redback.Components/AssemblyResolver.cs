namespace Redback.Components
{
    // Not needed for net48: .NET Framework 4.8 provides all BCL assemblies natively,
    // and Grasshopper finds plugin DLLs in the GH Libraries folder via its own probe path.
    static class AssemblyResolver { }
}
